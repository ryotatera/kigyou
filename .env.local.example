# Local 開発用。このファイルを `.env.local` にコピーして編集。
# `.env.local` は git にコミットされません（.gitignore 済み）。

NEXT_PUBLIC_SUPABASE_URL=https://irrrfxapdmphyutfjpch.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...（Supabase ダッシュボード → Settings → API → anon public）

# プロジェクトページで配信する場合のみ。User/Org page と独自ドメインでは不要。
# NEXT_PUBLIC_BASE_PATH=/kigyou
